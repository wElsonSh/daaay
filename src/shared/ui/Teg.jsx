export function Teg({ teg }) {
    return (
        <span className="ml-[-10px] w-10 overflow-hidden h-10 left-0   bg-neutral-500 text-neutral-100 flex rounded-full items-center justify-center hover:w-30 transition-all text-start">#{teg}</span>
    );
}